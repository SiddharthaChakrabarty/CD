#include <stdio.h>

void main() {
    int x = 0, y = 1;
    
    for (x = 0; x < 5; x++) {
        printf("Hello");
    }

    while (y < 5) {
        printf("World");
        y++;
    }

    do {
        printf("!");
    } while (x > 0);
}
